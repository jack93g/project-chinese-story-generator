version: 2

updates:
  # Keeps the SHA-pinned actions in .github/workflows/ current: Dependabot
  # opens a PR bumping both the SHA and its version comment.
  - package-ecosystem: github-actions
    directory: /
    target-branch: dev
    schedule:
      interval: weekly
