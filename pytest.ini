[pytest]
testpaths = tests
pythonpath = .
markers =
    db: tests that require a database connection
    smoke: tests that make live calls to external providers (skipped by default; set RUN_SMOKE_TESTS=1 to run)